---
layout: post
title: "Mi lista de animes"
tags: [Others, Collection, Animes]
read: true
toc: true
icon: "reading.svg"
keywords: "anime viendo mirando favoritos lista mi preferido recomendado recomiendo"
permalink: "/animes/"
noOutdated: true
---

{% markdown %}
Acá les dejo una lista de mis animes que he visto, y también los que recomiendo. Quiero aclarar que mi género preferido es el romance!
{% endmarkdown %}

<ol id="reading-list" class="indent" reversed>
  {% for item in books %}
    <li class="item">
      {% if item.reading %}
        <span class="badge badge-reading">Estoy viendo</span>{% endif %}
      {% if item.mychoice %}
        <span class="badge badge-my-choice">Recomendado!</span>{% endif %}
      {% if item.goodreads %}
        <a class="title" href="{{ item.goodreads }}" target="_blank">{{ item.title }}</a>
      {% else %}
        <span class="title">{{ item.title }}</span>{% endif %}
      <span class="author"> &mdash; {{ item.author }}.</span>
      {% if item.intro %}
        <span class="intro">{{ item.intro }}</span>{% endif %}
    </li>
  {% endfor %}
</ol>